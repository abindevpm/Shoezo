<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SHOEZOO - Track Order</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;600;700&display=swap');

        /* CLEAN MINIMAL THEME */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Outfit', sans-serif;
        }

        body {
            background: #fcfcfc;
            color: #111;
            line-height: 1.6;
        }

        .container {
            display: flex;
            width: 100%;
            margin: 0 auto;
            min-height: 100vh;
        }

        main {
            flex: 1;
            padding: 60px 80px;
            background: #fff;
        }

        /* PAGE HEADER */
        .page-header {
            margin-bottom: 50px;
            padding-bottom: 25px;
            border-bottom: 1px solid #eee;
        }

        .section-title {
            font-size: 30px;
            font-weight: 700;
            color: #000;
            letter-spacing: -0.8px;
            margin-bottom: 10px;
        }

        /* TRACK CARD */
        .track-card {
            background: #fff;
            border: 1px solid #eee;
            border-radius: 20px;
            overflow: hidden;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.02);
        }

        .track-header {
            padding: 30px 40px;
            background: #fcfcfc;
            border-bottom: 1px solid #eee;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .order-meta h3 {
            font-size: 18px;
            font-weight: 700;
            color: #000;
            margin-bottom: 5px;
        }

        .order-meta p {
            font-size: 12px;
            color: #888;
            text-transform: uppercase;
            font-weight: 800;
            letter-spacing: 1px;
        }

        .status-badge {
            padding: 8px 20px;
            border-radius: 4px;
            font-size: 10px;
            font-weight: 800;
            text-transform: uppercase;
            letter-spacing: 1.2px;
            background: #000;
            color: #fff;
            border: 1px solid #000;
        }

        /* STEPPER */
        .stepper-container {
            padding: 60px 40px;
            position: relative;
            border-bottom: 1px solid #f9f9f9;
        }

        .stepper {
            display: flex;
            justify-content: space-between;
            position: relative;
            z-index: 2;
        }

        .step {
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 15px;
            flex: 1;
        }

        .step-icon {
            width: 45px;
            height: 45px;
            border-radius: 50%;
            background: #fff;
            border: 1px solid #eee;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 14px;
            color: #ddd;
            font-weight: 700;
            transition: all 0.3s ease;
        }

        .step.active .step-icon {
            background: #000;
            color: #fff;
            border-color: #000;
            box-shadow: 0 0 0 5px rgba(0, 0, 0, 0.05);
        }

        .step.completed .step-icon {
            background: #fff;
            color: #000;
            border-color: #000;
        }

        .step-label {
            font-size: 11px;
            font-weight: 800;
            text-transform: uppercase;
            letter-spacing: 1px;
            color: #ddd;
        }

        .step.active .step-label {
            color: #000;
        }

        .step.completed .step-label {
            color: #888;
        }

        .progress-line {
            position: absolute;
            top: 82px;
            left: 10%;
            right: 10%;
            height: 2px;
            background: #eee;
            z-index: 1;
        }

        .progress-fill {
            height: 100%;
            background: #000;
            transition: width 0.8s cubic-bezier(0.4, 0, 0.2, 1);
        }

        /* BANNERS */
        .status-banner {
            padding: 20px 40px;
            margin: 40px;
            border-radius: 12px;
            display: flex;
            align-items: center;
            gap: 15px;
            font-weight: 700;
            font-size: 13px;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .cancelled-banner {
            background: #fff5f5;
            color: #dc2626;
            border: 1px solid #fee2e2;
        }

        .returned-banner {
            background: #f8f9fa;
            color: #666;
            border: 1px solid #eee;
        }

        /* TRACK DETAILS */
        .track-grid {
            display: grid;
            grid-template-columns: 1.2fr 0.8fr;
            gap: 60px;
            padding: 40px;
        }

        .details-section h4 {
            font-size: 14px;
            font-weight: 800;
            text-transform: uppercase;
            letter-spacing: 1.5px;
            margin-bottom: 25px;
            color: #000;
            padding-bottom: 10px;
            border-bottom: 1px solid #eee;
        }

        .item-row {
            display: flex;
            gap: 20px;
            margin-bottom: 20px;
            padding-bottom: 20px;
            border-bottom: 1px solid #f9f9f9;
        }

        .item-img {
            width: 80px;
            height: 80px;
            background: #fff;
            border: 1px solid #eee;
            border-radius: 10px;
            padding: 8px;
        }

        .item-img img {
            width: 100%;
            height: 100%;
            object-fit: contain;
        }

        .item-info h5 {
            font-size: 15px;
            font-weight: 700;
            color: #000;
            margin-bottom: 5px;
        }

        .item-info p {
            font-size: 12px;
            color: #888;
        }

        .item-info .price {
            font-size: 14px;
            font-weight: 700;
            color: #000;
            margin-top: 8px;
            display: block;
        }

        .total-box {
            margin-top: 30px;
            padding: 25px;
            background: #fafafa;
            border-radius: 12px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .total-box span {
            font-size: 12px;
            color: #888;
            font-weight: 700;
            text-transform: uppercase;
        }

        .total-box strong {
            font-size: 20px;
            font-weight: 800;
            color: #000;
        }

        .info-card {
            background: #fff;
            border: 1px solid #eee;
            border-radius: 14px;
            padding: 25px;
            margin-bottom: 30px;
        }

        .info-card p {
            font-size: 14px;
            color: #555;
            line-height: 1.7;
            margin-bottom: 5px;
        }

        /* FOOTER ACTIONS */
        .track-footer {
            padding: 30px 40px;
            background: #fcfcfc;
            border-top: 1px solid #eee;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .back-btn {
            font-size: 12px;
            font-weight: 800;
            color: #000;
            text-decoration: none;
            text-transform: uppercase;
            letter-spacing: 1px;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .help-link {
            font-size: 12px;
            font-weight: 700;
            color: #888;
            text-decoration: none;
        }

        /* RESPONSIVE */
        @media (max-width: 1024px) {
            .track-grid {
                grid-template-columns: 1fr;
            }
        }

        /* SIDEBAR STYLES */
        aside {
            width: 280px;
            border-right: 1px solid #eee;
            padding-top: 30px;
            flex-shrink: 0;
            background: #fff;
        }

        .user-header {
            background: #000;
            color: #fff;
            padding: 15px;
            display: flex;
            align-items: center;
            gap: 15px;
            margin-bottom: 5px;
        }

        .user-header img {
            width: 50px;
            height: 50px;
            border-radius: 50%;
            border: 2px solid #fff;
            object-fit: cover;
        }

        .menu-item {
            display: flex;
            align-items: center;
            gap: 15px;
            padding: 15px;
            text-decoration: none;
            color: #555;
            font-size: 14px;
            border-bottom: 1px solid #eee;
        }

        .menu-item:hover,
        .menu-item.active {
            background-color: #f5f5f5;
            color: #000;
        }

        .menu-item i {
            width: 20px;
            text-align: center;
        }
    </style>
</head>

<body>
    <%- include("../partials/userpartial/header.ejs") %>

        <div class="container">
            <%- include("../partials/userpartial/userprofile-sidebar.ejs", { user: user, activePage: "orders" }) %>

                <main>
                    <div class="page-header">
                        <h2 class="section-title">Order Tracking</h2>
                    </div>

                    <div class="track-card">
                        <div class="track-header">
                            <div class="order-meta">
                                <h3>Order #<%= order.orderId %>
                                </h3>
                                <p>Placed on <%= new Date(order.createdAt).toLocaleDateString('en-IN', { day: 'numeric'
                                        , month: 'long' , year: 'numeric' }) %>
                                </p>
                            </div>
                            <span class="status-badge">
                                <%= order.status %>
                            </span>
                        </div>

                        <% const steps=['Placed', 'Processing' , 'Shipped' , 'Delivered' ]; const
                            currentIndex=steps.indexOf(order.status); const isCancelled=order.status==='Cancelled' ;
                            const isReturned=order.status==='Returned' ; %>

                            <% if (isCancelled) { %>
                                <div class="status-banner cancelled-banner">
                                    <i class="fas fa-times-circle"></i>
                                    This order has been cancelled and a refund (if applicable) has been initiated.
                                </div>
                                <% } else if (isReturned) { %>
                                    <div class="status-banner returned-banner">
                                        <i class="fas fa-undo"></i>
                                        This order has been returned successfully.
                                    </div>
                                    <% } else { %>
                                        <div class="stepper-container">
                                            <div class="stepper">
                                                <% steps.forEach((step, index)=> { %>
                                                    <div
                                                        class="step <%= index === currentIndex ? 'active' : (index < currentIndex ? 'completed' : '') %>">
                                                        <div class="step-icon">
                                                            <% if (index < currentIndex) { %>
                                                                <i class="fas fa-check"></i>
                                                                <% } else { %>
                                                                    <%= index + 1 %>
                                                                        <% } %>
                                                        </div>
                                                        <span class="step-label">
                                                            <%= step %>
                                                        </span>
                                                    </div>
                                                    <% }) %>
                                            </div>
                                            <div class="progress-line">
                                                <div class="progress-fill"
                                                    style="width: <%= (currentIndex / (steps.length - 1)) * 100 %>%">
                                                </div>
                                            </div>
                                        </div>
                                        <% } %>

                                            <div class="track-grid">
                                                <div class="details-section">
                                                    <h4>Items in Order</h4>
                                                    <% order.items.forEach(item=> { %>
                                                        <div class="item-row">
                                                            <div class="item-img">
                                                                <img src="/uploads/product-images/<%= item.productId.images[0] %>"
                                                                    alt="<%= item.productId.name %>">
                                                            </div>
                                                            <div class="item-info">
                                                                <h5>
                                                                    <%= item.productId.name %>
                                                                </h5>
                                                                <p>Qty: <%= item.quantity %> | Size: <%= item.size %>
                                                                </p>
                                                                <span class="price">Γé╣<%= item.price.toLocaleString() %>
                                                                </span>
                                                            </div>
                                                        </div>
                                                        <% }) %>

                                                            <div class="total-box">
                                                                <span>Final Amount Paid</span>
                                                                <strong>Γé╣<%= order.totalAmount.toLocaleString() %>
                                                                </strong>
                                                            </div>
                                                </div>

                                                <div class="details-section">
                                                    <h4>Shipping Log</h4>
                                                    <div class="info-card">
                                                        <strong
                                                            style="display: block; font-size: 13px; margin-bottom: 10px;">
                                                            <%= order.address.fullName || order.address.name %>
                                                        </strong>
                                                        <p>
                                                            <%= order.address.addressLine %>
                                                        </p>
                                                        <p>
                                                            <%= order.address.city %>, <%= order.address.state %> - <%=
                                                                        order.address.pincode %>
                                                        </p>
                                                        <p style="margin-top: 15px; font-weight: 700;"><i
                                                                class="fas fa-phone"></i>
                                                            <%= order.address.phone %>
                                                        </p>
                                                    </div>

                                                    <h4>Payment Details</h4>
                                                    <div class="info-card">
                                                        <p>Method: <strong>
                                                                <%= order.paymentMethod %>
                                                            </strong></p>
                                                        <p>Status: <strong>
                                                                <%= order.paymentStatus || 'Successful' %>
                                                            </strong></p>
                                                    </div>
                                                </div>
                                            </div>

                                            <div class="track-footer">
                                                <a href="/orders" class="back-btn">
                                                    <i class="fas fa-arrow-left"></i> Back to History
                                                </a>
                                                <a href="#" class="help-link">Encountering Issues? Contact Support</a>
                                            </div>
                    </div>
                </main>
        </div>

        <%- include("../partials/userpartial/footer.ejs") %>
</body>

</html>
