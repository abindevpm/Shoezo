<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>404 - Page Not Found</title>

    <!-- Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- Inter Font (same as your original) -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;700;800&display=swap" rel="stylesheet">

    <style>
        body {
            font-family: 'Inter', sans-serif;
            background-color: #ffffff;
            min-height: 100vh;
            margin: 0;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .error-code {
            font-size: 12rem;
            font-weight: 800;
            line-height: 1;
            letter-spacing: -10px;
            font-family: 'Arial Black', Arial, sans-serif;
            color: #000;
        }

        @media (min-width: 640px) {
            .error-code {
                font-size: 15rem;
            }
        }

        .btn-home {
            background-color: #000;
            color: white;
            border: 2px solid #000;
            padding: 0.75rem 2rem;
            font-weight: 500;
            text-transform: uppercase;
            letter-spacing: 2px;
            border-radius: 8px;
            transition: all 0.3s ease;
        }

        .btn-home:hover {
            background-color: #1f2937;
            border-color: #1f2937;
        }
    </style>
</head>
<body>

<div class="container text-center py-5">
    <div class="row justify-content-center">
        <div class="col-12 col-lg-8">

            <p class="text-uppercase fw-semibold tracking-widest text-black mb-4" style="font-size: 0.875rem; letter-spacing: 0.2em;">
                Oops! Page Not Found
            </p>

            <div class="error-code mb-5">404</div>

            <p class="fs-5 fw-medium text-black text-uppercase tracking-wider mb-5" style="max-width: 500px; margin: 0 auto; line-height: 1.6;">
                We are sorry, but the page you requested was not found
            </p>

            <a href="/" class="btn btn-home">
                Return to Home Page
            </a>

        </div>
    </div>
</div>

<!-- Bootstrap JS (optional) -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>