<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SHOEZO - Forgot Password</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Geist:wght@400;500;600;700&display=swap');
        
        body {
            font-family: 'Geist', sans-serif;
        }
        
        .italic-brand {
            font-style: italic;
            font-weight: 700;
        }
    </style>
</head>
<body class="bg-white">
    <div class="flex h-screen">
        <!-- Left Section - Product Showcase -->
        <div class="hidden md:flex w-1/2 bg-gray-700 items-center justify-center relative overflow-hidden">
            <!-- Product Image -->
            <img 
                src="/images/forgotpassword.jpg" 
                alt="Black Adidas Shoe with White Stripes" 
                class="w-full h-full object-cover"
            >
        </div>
        
        <!-- Right Section - Forgot Password Form -->
        <div class="w-full md:w-1/2 flex flex-col justify-center px-8 md:px-16 py-12 bg-white">
            <div class="max-w-sm mx-auto w-full">
                <!-- Brand Logo -->
                <h1 class="text-4xl md:text-5xl font-bold italic-brand text-black mb-8">SHOEZO</h1>
                
                <!-- Forgot Password Heading -->
                <h2 class="text-2xl font-bold text-black mb-4">Forgot password</h2>
                
                <!-- Description -->
                <p class="text-gray-600 text-sm mb-8">Please enter your Email to reset the password</p>
                
                <!-- Forgot Password Form -->
                <form action="/reset-password" method="POST" class="space-y-6">
                    <!-- Email Label -->
                    <div>
                        <label for="email" class="block text-sm font-medium text-gray-900 mb-2">email</label>
                        
                        <!-- Email Input -->
                        <input 
                            type="email" 
                            id="email"
                            name="email" 
                            placeholder="dbcd@gmail.com" 
                            required
                            class="w-full px-4 py-3 border-2 border-gray-300 text-gray-700 placeholder-gray-400 rounded-lg focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-200 transition"
                        >
                    </div>
                    
                    <!-- Reset Password Button -->
                    <button 
                        type="submit" 
                        class="w-full bg-blue-500 hover:bg-blue-600 text-white font-bold py-3 px-6 rounded-lg transition duration-200 transform hover:scale-105"
                    >
                        Reset Password
                    </button>
                </form>
                
                <!-- Back to Login Link -->
                <div class="text-center mt-6">
                    <a href="/login" class="text-sm text-gray-600 hover:text-gray-800 font-medium">Back to Login</a>
                </div>
            </div>
        </div>
    </div>
</body>
</html>